import { type PasskeyMessages } from '@dynamic-labs/message-transport';
import { Core } from '../../client/core';
type PasskeyModule = {
    authenticateMFA: PasskeyMessages['authenticatePasskeyMFA'];
    get: PasskeyMessages['getPasskeys'];
    register: PasskeyMessages['registerPasskey'];
};
export declare const createPasskeysModule: (core: Core) => PasskeyModule;
export {};
