import type { CombinedWallet } from '../hooks/useCombinedWallets';
interface WalletInfoDisplayProps {
    selectedWallet: CombinedWallet | null;
    title?: string;
    message: string;
}
export declare const WalletInfoDisplay: ({ selectedWallet, title, message, }: WalletInfoDisplayProps) => import("react/jsx-runtime").JSX.Element;
export {};
