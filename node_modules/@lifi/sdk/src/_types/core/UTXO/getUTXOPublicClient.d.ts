import type { Account, Chain, Client, FallbackTransport, HttpTransport, PublicActions, UTXOSchema, WalletActions } from '@bigmi/core';
type PublicClient = Client<FallbackTransport<readonly HttpTransport[]>, Chain, Account | undefined, UTXOSchema, PublicActions & WalletActions>;
/**
 * Get an instance of a provider for a specific chain
 * @param chainId - Id of the chain the provider is for
 * @returns The public client for the given chain
 */
export declare const getUTXOPublicClient: (chainId: number) => Promise<PublicClient>;
export {};
//# sourceMappingURL=getUTXOPublicClient.d.ts.map