import type { DynamicClient } from '../../../../../client/types';
import type { User } from '../../../../user/types';
type CompleteSocialRedirectSignInParams = {
    url: URL;
};
/** Completes the social redirect sign in flow. */
export declare const completeSocialRedirectSignIn: (client: DynamicClient, { url }: CompleteSocialRedirectSignInParams) => Promise<User | null>;
export {};
//# sourceMappingURL=completeSocialRedirectSignIn.d.ts.map