import type { DynamicClient } from '../../../../../client/types';
type DetectOAuthRedirectParams = {
    url: URL;
};
/** Detects if the URL is a redirect from an OAuth provider. */
export declare const detectOAuthRedirect: (client: DynamicClient, { url }: DetectOAuthRedirectParams) => Promise<boolean>;
export {};
//# sourceMappingURL=detectOAuthRedirect.d.ts.map