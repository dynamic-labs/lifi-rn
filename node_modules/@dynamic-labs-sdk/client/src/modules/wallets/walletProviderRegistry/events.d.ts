import type { WalletProvider } from '../walletProvider/types';
declare global {
    interface DynamicEvents {
        /**
         * Event emitted when an wallet provider is registered, updated or unregistered
         */
        'walletProviderChanged': (key: string) => void;
        /**
         * Event emitted every time a wallet provider is registered
         */
        'walletProviderRegistered': (value: WalletProvider) => void;
    }
}
export {};
//# sourceMappingURL=events.d.ts.map