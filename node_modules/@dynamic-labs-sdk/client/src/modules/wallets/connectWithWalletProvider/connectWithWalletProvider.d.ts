import type { WalletAccount } from '../walletAccount';
import type { WalletProvider } from '../walletProvider';
/**
 * Connects to a wallet provider and returns the connected wallet account.
 *
 * Throws the NoAddressFoundError error in case the request to connect was successful but no
 * address is connected to the dapp.
 */
export declare const connectWithWalletProvider: (walletProvider: WalletProvider) => Promise<WalletAccount>;
//# sourceMappingURL=connectWithWalletProvider.d.ts.map