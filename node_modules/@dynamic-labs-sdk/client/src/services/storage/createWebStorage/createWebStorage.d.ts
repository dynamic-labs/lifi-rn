import type { Storage } from '../types';
type CreateWebStorageParams = {
    prefix?: string;
};
/**
 * Creates a Storage instance that uses localStorage as the storage model meant for use in the browser.
 */
export declare const createWebStorage: ({ prefix, }?: CreateWebStorageParams) => Storage;
export {};
//# sourceMappingURL=createWebStorage.d.ts.map